<div class="container">
<% if current_user %>
  <h1>Welcome <%= current_user.email %></h1>
<% else %>
  <h1>Welcome</h1>
<% end %>


<% if current_user %>
  <%= link_to 'Sign Out', destroy_user_session_path, :data => {:method=>'delete'} %>
<% else %>
  <h4>Login:</h4>
<%= link_to 'Login', new_user_session_path %>
<h4> Sign Up:</h4>
<%= link_to 'Create Account', new_user_registration_path %>
  <br>
<% end %>

<h4>Project Hiring Assessment</h4>
  <%= link_to 'Hiring Assessment Quiz', quiz_intro_path %>
<% if is_admin? %>
<h4>Project Hiring Assessment Results</h4>
  <%= link_to 'Results', hiring_results_path %>
<% end %>

<h4>D & I Assessment</h4>
  <%= link_to 'D&I Quiz', diversity_intro_path %>
</div>
