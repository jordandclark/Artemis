
<% if current_user %>
  <h1>Welcome <%= current_user.email %></h1>
<% else %>
  <h1>Welcome</h1>
<% end %>


<% if current_user %>
  <%= link_to 'Admin Sign Out', destroy_user_session_path, :data => {:method=>'delete'} %>
<% else %>
  <h4>Admin Login:</h4>
<%= link_to 'Admin Login', new_user_session_path %>
  <br>
<% end %>

<h4>Project Hiring Assessment</h4>
  <%= link_to 'Hiring Assessment Quiz', new_assessment_path %>
<% if is_admin? %>
<h4>Project Hiring Assessment Results</h4>
  <%= link_to 'Results', admins_path %>
<% end %>

<h4>D & I Assessment</h4>
  <%= link_to 'D&I Quiz', new_diversity_path %>
